[package]
name = "layer-merge"
version = "0.1.0"
edition = "2021"
description = "JSON CLI that merges OCI-style image layers (whiteouts & opaque directories)"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = 2
